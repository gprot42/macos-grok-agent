{
  "identifier": "agent-tools",
  "description": "Enable tool calling for web search (http), code exec (shell), and file ops (fs) via Tauri",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "fs:read-all",
    "fs:write-all",
    "http:default",
    "shell:allow-execute"
  ]
}